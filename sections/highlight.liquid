<div class="highlight-section">
  <div class="highlight-wrapper">
    {% for block in section.blocks %}
      <div class="highlight-block" style="background-color: {{ block.settings.block_color | default: 'white' }};" {{ block.shopify_attributes }}>
        {% if block.settings.text %}
          <div class="highlight-block-text">{{ block.settings.text }}</div>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>

<style>
  .highlight-section {
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    background-color: {{ section.settings.background_color | default: '#0b00b5' }};
    height: {{ section.settings.height | default: 700 }}px;
  }

  .highlight-wrapper {
    width: 100%;
    height: 100%;
    display: flex;
    padding: 48px;
    gap: 48px;
    box-sizing: border-box;
  }

  .highlight-block {
    flex: 1;
    background-color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100%;
  }

  .highlight-block-text {
    font-family: 'Noto Sans JP', 'Hiragino Sans', 'Hiragino Kaku Gothic ProN', Meiryo, sans-serif;
    writing-mode: vertical-lr;
    text-orientation: upright;
    color: black;
    font-weight: bold;
    white-space: nowrap;
    line-height: 100%;
    display: flex;
    align-items: center;
  }
</style>

{% schema %}
{
  "name": "Highlight",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Kolor tła",
      "default": "#0b00b5"
    },
    {
      "type": "number",
      "id": "height",
      "label": "Wysokość (px)",
      "default": 700
    }
  ],
  "blocks": [
    {
      "type": "sub_section",
      "name": "Sub-sekcja",
      "settings": [
        {
          "type": "color",
          "id": "block_color",
          "label": "Kolor bloku",
          "default": "#ffffff"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Tekst",
          "default": "日本語"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Highlight"
    }
  ]
}
{% endschema %}
