{% comment %}
  Text Slide Medium Snippet
  Renders a medium text slide (2x small + 12px gap)
{% endcomment %}

{% if block.settings.link %}
  <a href="{{ block.settings.link }}" class="slide-link">
{% endif %}

<div class="slide-medium" style="background-color: {{ block.settings.background_color }}; color: {{ block.settings.text_color }};">
  <div class="slide-text-content">
    {% if block.settings.heading != blank or block.settings.text != blank %}
      {% if block.settings.heading != blank %}
        <h2 class="slide-heading">{{ block.settings.heading }}</h2>
      {% endif %}
      
      {% if block.settings.text != blank %}
        <div class="slide-text-body">
          {{ block.settings.text }}
        </div>
      {% endif %}
    {% else %}
      {% comment %} Empty state - no content {% endcomment %}
      <div class="empty-state">
        <div class="empty-state-icon">📝</div>
        <div class="empty-state-text">No content added</div>
        <div class="empty-state-subtext">Add heading and text in the theme editor</div>
      </div>
    {% endif %}
  </div>
</div>

{% if block.settings.link %}
  </a>
{% endif %}

<style>
  .slide-medium {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 40px;
    text-align: center;
  }

  .slide-text-content {
    max-width: 100%;
  }

  .slide-heading {
    font-size: 36px;
    font-weight: bold;
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .slide-text-body {
    font-size: 18px;
    line-height: 1.6;
  }

  .slide-text-body p {
    margin-bottom: 15px;
  }

  .slide-text-body p:last-child {
    margin-bottom: 0;
  }

  .slide-link {
    display: block;
    width: 100%;
    height: 100%;
    text-decoration: none;
    color: inherit;
  }

  .slide-link:hover {
    text-decoration: none;
  }
  
  /* Empty state styles */
  .empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: #666;
    opacity: 0.7;
  }
  
  .empty-state-icon {
    font-size: 48px;
    margin-bottom: 16px;
    opacity: 0.5;
  }
  
  .empty-state-text {
    font-size: 18px;
    font-weight: 500;
    margin-bottom: 8px;
  }
  
  .empty-state-subtext {
    font-size: 14px;
    opacity: 0.7;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .slide-medium {
      padding: 30px;
    }
    
    .slide-heading {
      font-size: 28px;
    }
    
    .slide-text-body {
      font-size: 16px;
    }
    
    .empty-state-icon {
      font-size: 36px;
    }
    
    .empty-state-text {
      font-size: 16px;
    }
    
    .empty-state-subtext {
      font-size: 12px;
    }
  }

  @media (max-width: 768px) {
    .slide-medium {
      padding: 20px;
    }
    
    .slide-heading {
      font-size: 24px;
    }
    
    .slide-text-body {
      font-size: 14px;
    }
    
    .empty-state-icon {
      font-size: 28px;
    }
    
    .empty-state-text {
      font-size: 14px;
    }
    
    .empty-state-subtext {
      font-size: 11px;
    }
  }
</style>
