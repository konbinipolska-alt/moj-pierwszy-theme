{% comment %}
  Text Slide Wide Snippet
  Renders a wide text slide (2x normal width + gap)
{% endcomment %}

{% if block.settings.link %}
  <a href="{{ block.settings.link }}" class="slide-link">
{% endif %}

<div class="slide-text text-slide-wide" style="background-color: {{ block.settings.background_color }}; color: {{ block.settings.text_color }};">
  <div class="slide-text-content">
    {% if block.settings.heading != blank or block.settings.text != blank %}
      {% if block.settings.heading != blank %}
        <h2 class="slide-heading">{{ block.settings.heading }}</h2>
      {% endif %}
      
      {% if block.settings.text != blank %}
        <div class="slide-text-body">
          {{ block.settings.text }}
        </div>
      {% endif %}
    {% else %}
      {% comment %} Empty state - no content {% endcomment %}
      <div class="empty-state">
        <div class="empty-state-icon">📝</div>
        <div class="empty-state-text">No content added</div>
        <div class="empty-state-subtext">Add heading and text in the theme editor</div>
      </div>
    {% endif %}
  </div>
</div>

{% if block.settings.link %}
  </a>
{% endif %}

<style>
  .slide-text {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 60px;
    text-align: center;
  }

  .slide-text-content {
    max-width: 100%;
  }

  .slide-heading {
    font-size: 48px;
    font-weight: bold;
    margin-bottom: 24px;
    line-height: 1.2;
  }

  .slide-text-body {
    font-size: 22px;
    line-height: 1.6;
  }

  .slide-text-body p {
    margin-bottom: 18px;
  }

  .slide-text-body p:last-child {
    margin-bottom: 0;
  }

  .slide-link {
    display: block;
    width: 100%;
    height: 100%;
    text-decoration: none;
    color: inherit;
  }

  .slide-link:hover {
    text-decoration: none;
  }
  
  /* Empty state styles */
  .empty-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: #666;
    opacity: 0.7;
  }
  
  .empty-state-icon {
    font-size: 64px;
    margin-bottom: 20px;
    opacity: 0.5;
  }
  
  .empty-state-text {
    font-size: 24px;
    font-weight: 500;
    margin-bottom: 12px;
  }
  
  .empty-state-subtext {
    font-size: 18px;
    opacity: 0.7;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .slide-text {
      padding: 40px;
    }
    
    .slide-heading {
      font-size: 36px;
    }
    
    .slide-text-body {
      font-size: 20px;
    }
    
    .empty-state-icon {
      font-size: 48px;
    }
    
    .empty-state-text {
      font-size: 20px;
    }
    
    .empty-state-subtext {
      font-size: 16px;
    }
  }

  @media (max-width: 768px) {
    .slide-text {
      padding: 30px;
    }
    
    .slide-heading {
      font-size: 28px;
    }
    
    .slide-text-body {
      font-size: 18px;
    }
    
    .empty-state-icon {
      font-size: 36px;
    }
    
    .empty-state-text {
      font-size: 16px;
    }
    
    .empty-state-subtext {
      font-size: 14px;
    }
  }
</style>
